# TerminalSafari
TerminalSafari is a high-performance, persistent, and networked terminal emulator that runs natively in Safari. Powered by WebAssembly (WASM) and the Wasmer SDK, it brings a real Bash-like environment to iOS and macOS without the need for a backend server.
